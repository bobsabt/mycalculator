const Screen = ({value}) => {
    return (
        <div className="screen-container">
            <div className="screen-content">    
            <h1>{value}</h1>
            </div>
        </div>
    )
};

export default Screen;
